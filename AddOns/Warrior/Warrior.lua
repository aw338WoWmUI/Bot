doWhenGMRIsFullyLoaded(function ()
  local yielder = createYielder()
  while true do
    if GMR.InCombat() then
      local spellReflection =

      -- Heroic Leap
    end
    yielder.yield()
  end
end)
